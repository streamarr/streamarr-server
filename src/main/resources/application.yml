spring:
  flyway:
    table: schema_history
    enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5432/streamarr
    username: admin
    password: pass

  profiles:
    active: dev

  threads:
    virtual:
      enabled: true

dgs:
  graphql:
    graphiql:
      enabled: false
    introspection:
      enabled: false

tmdb:
  api:
    token: ${TMDB_API_TOKEN:}

streaming:
  max-concurrent-transcodes: ${STREAMING_MAX_CONCURRENT_TRANSCODES:8}
  segment-duration-seconds: ${STREAMING_SEGMENT_DURATION:6}
  session-timeout-seconds: ${STREAMING_SESSION_TIMEOUT:60}
