type Episode {
    id: ID!
    title: String
    episodeNumber: Int!
    overview: String
    stillPath: String
    airDate: String
    runtime: Int
    files: [MediaFile]!
    images(type: ImageType): [Image]!
}
