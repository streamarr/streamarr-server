// Streamarr authorization schema (ADR 0025). Entity attributes are facts the slice builders load;
// optional attributes are present only for actions whose builders load them, so a policy that needs
// a fact must test `has` before reading it — an absent fact reads as "not proven", never as "true".
namespace Streamarr {
  // The authenticated Account. Signed facts (membership Household, role, context Household,
  // selected Profile) come from the token; live facts (enabled, serverAdmin, session liveness,
  // whether the context Household may still be used) are read from PostgreSQL per action.
  entity Account {
    enabled?: Bool,
    serverAdmin?: Bool,
    householdRole?: String,
    membershipHousehold?: Household,
    contextHousehold?: Household,
    selectedProfile?: Profile,
    contextHouseholdUsable?: Bool,
    sessionLive?: Bool,
    liveHouseholdRole?: String,
    liveMembershipHousehold?: Household,
    eligible?: Bool,
    deviceBound?: Bool,
  };

  entity Household;

  // A Profile as seen from the principal's vantage point for one decision.
  entity Profile {
    availableInContext?: Bool,
    locked?: Bool,
    pinRequired?: Bool,
    managedByPrincipal?: Bool,
    availableInPrincipalHousehold?: Bool,
    supervisedByPrincipal?: Bool,
    unlinked?: Bool,
    shareFree?: Bool,
    principalSoleManager?: Bool,
    offerableByPrincipal?: Bool,
    directlyManagedByPrincipal?: Bool,
    sovereignPersonalProfileOfPrincipal?: Bool,
  };

  // One Profile's share into one Household, as the principal relates to it for one decision.
  entity Share {
    structural?: Bool,
    offeredByPrincipal?: Bool,
    principalMemberOfTarget?: Bool,
    principalAdminOfTarget?: Bool,
    directlyManagedByPrincipal?: Bool,
    sovereignOverProfileByPrincipal?: Bool,
  };

  // A pending manager invitation's parties relative to the principal for one decision.
  entity ManagerInvitation {
    recipientIsPrincipal?: Bool,
    invitedByPrincipal?: Bool,
  };

  // A pairing grant, resolved from the typed user code before Cedar ever sees the request.
  entity Grant;

  // A registered TV's Household binding relative to the principal for one decision.
  entity Registration {
    principalAdminOfHousehold?: Bool,
  };

  // The singleton root resource server-wide authority is granted on.
  entity Server;

  action serverAdministration;
  action libraryAdministration in serverAdministration;
  action householdAdministration in serverAdministration;
  action accountAdministration;
  action profileAdministration;

  action addLibrary in libraryAdministration appliesTo {
    principal: Account,
    resource: Server,
    context: {},
  };
  action removeLibrary in libraryAdministration appliesTo {
    principal: Account,
    resource: Server,
    context: {},
  };
  action scanLibrary in libraryAdministration appliesTo {
    principal: Account,
    resource: Server,
    context: {},
  };
  action refreshLibrary in libraryAdministration appliesTo {
    principal: Account,
    resource: Server,
    context: {},
  };

  action viewProfilePicker appliesTo {
    principal: Account,
    resource: Household,
    context: {},
  };
  action selectProfile appliesTo {
    principal: Account,
    resource: Profile,
    context: { pinVerified: Bool },
  };
  action playback appliesTo {
    principal: Account,
    resource: Profile,
    context: {},
  };
  action viewProfileActivity appliesTo {
    principal: Account,
    resource: Profile,
    context: {},
  };
  action viewHouseholdAdministration in householdAdministration appliesTo {
    principal: Account,
    resource: Household,
    context: {},
  };
  action viewAccountAdministration in accountAdministration appliesTo {
    principal: Account,
    resource: Account,
    context: {},
  };
  action grantServerAdmin in accountAdministration appliesTo {
    principal: Account,
    resource: Account,
    context: { freshReauthentication: Bool },
  };
  action revokeServerAdmin in accountAdministration appliesTo {
    principal: Account,
    resource: Account,
    context: { freshReauthentication: Bool },
  };
  action grantHouseholdAdmin in accountAdministration appliesTo {
    principal: Account,
    resource: Account,
    context: {},
  };
  action revokeHouseholdAdmin in accountAdministration appliesTo {
    principal: Account,
    resource: Account,
    context: {},
  };
  action disableAccount in accountAdministration appliesTo {
    principal: Account,
    resource: Account,
    context: {},
  };
  action enableAccount in accountAdministration appliesTo {
    principal: Account,
    resource: Account,
    context: {},
  };
  action renameAccount in accountAdministration appliesTo {
    principal: Account,
    resource: Account,
    context: {},
  };
  action createHousehold in householdAdministration appliesTo {
    principal: Account,
    resource: Server,
    context: {},
  };
  action viewHouseholds in householdAdministration appliesTo {
    principal: Account,
    resource: Server,
    context: {},
  };
  action renameHousehold in householdAdministration appliesTo {
    principal: Account,
    resource: Household,
    context: {},
  };
  action createProfile in profileAdministration appliesTo {
    principal: Account,
    resource: Household,
    context: {},
  };
  action createProfileWithLocalManager in profileAdministration appliesTo {
    principal: Account,
    resource: Household,
    context: {},
  };
  action editProfile in profileAdministration appliesTo {
    principal: Account,
    resource: Profile,
    context: {},
  };
  action changeProfileKind in profileAdministration appliesTo {
    principal: Account,
    resource: Profile,
    context: {},
  };
  action liftFinalRestriction in profileAdministration appliesTo {
    principal: Account,
    resource: Profile,
    context: { freshReauthentication: Bool },
  };
  action restrictSovereignAdult in profileAdministration appliesTo {
    principal: Account,
    resource: Profile,
    context: { freshReauthentication: Bool },
  };
  action manageProfilePin in profileAdministration appliesTo {
    principal: Account,
    resource: Profile,
    context: {},
  };
  action administrativelyResetProfilePin in profileAdministration appliesTo {
    principal: Account,
    resource: Profile,
    context: { freshReauthentication: Bool },
  };
  action deleteProfile in profileAdministration appliesTo {
    principal: Account,
    resource: Profile,
    context: { freshReauthentication: Bool },
  };
  action invitationAdministration in serverAdministration;
  action issueAccountInvitation in invitationAdministration appliesTo {
    principal: Account,
    resource: Server,
    context: {},
  };
  action cancelAccountInvitation in invitationAdministration appliesTo {
    principal: Account,
    resource: Server,
    context: {},
  };
  action viewAccountInvitations in invitationAdministration appliesTo {
    principal: Account,
    resource: Server,
    context: {},
  };
  action issuePasswordReset in accountAdministration appliesTo {
    principal: Account,
    resource: Account,
    context: { freshReauthentication: Bool },
  };
  action shareAdministration;
  action offerProfileShare in shareAdministration appliesTo {
    principal: Account,
    resource: Profile,
    context: {},
  };
  action acceptProfileShare in shareAdministration appliesTo {
    principal: Account,
    resource: Share,
    context: {},
  };
  action rejectProfileShare in shareAdministration appliesTo {
    principal: Account,
    resource: Share,
    context: {},
  };
  action cancelProfileShare in shareAdministration appliesTo {
    principal: Account,
    resource: Share,
    context: {},
  };
  action endProfileShare in shareAdministration appliesTo {
    principal: Account,
    resource: Share,
    context: {},
  };
  action administrativelyEndProfileShare in shareAdministration appliesTo {
    principal: Account,
    resource: Share,
    context: { freshReauthentication: Bool },
  };
  action managerAdministration;
  action inviteProfileManager in managerAdministration appliesTo {
    principal: Account,
    resource: Profile,
    context: {},
  };
  action viewManagerInvitations in managerAdministration appliesTo {
    principal: Account,
    resource: Profile,
    context: {},
  };
  action acceptManagerInvitation in managerAdministration appliesTo {
    principal: Account,
    resource: ManagerInvitation,
    context: {},
  };
  action declineManagerInvitation in managerAdministration appliesTo {
    principal: Account,
    resource: ManagerInvitation,
    context: {},
  };
  action cancelManagerInvitation in managerAdministration appliesTo {
    principal: Account,
    resource: ManagerInvitation,
    context: {},
  };
  action relinquishProfileManagement in managerAdministration appliesTo {
    principal: Account,
    resource: Profile,
    context: {},
  };
  action removeProfileManager in managerAdministration appliesTo {
    principal: Account,
    resource: Profile,
    context: {},
  };
  action administrativelyGrantProfileManager in managerAdministration appliesTo {
    principal: Account,
    resource: Profile,
    context: { freshReauthentication: Bool },
  };
  action administrativelyRemoveProfileManager in managerAdministration appliesTo {
    principal: Account,
    resource: Profile,
    context: { freshReauthentication: Bool },
  };
  action deviceAdministration;
  action linkDevice in deviceAdministration appliesTo {
    principal: Account,
    resource: Grant,
    context: {},
  };
  action revokeDeviceRegistration in deviceAdministration appliesTo {
    principal: Account,
    resource: Registration,
    context: {},
  };
  action blockEsn in deviceAdministration appliesTo {
    principal: Account,
    resource: Household,
    context: {},
  };
  action blockEsnServerWide in deviceAdministration appliesTo {
    principal: Account,
    resource: Server,
    context: { freshReauthentication: Bool },
  };
  action unblockEsn in deviceAdministration appliesTo {
    principal: Account,
    resource: Household,
    context: {},
  };
  action unblockEsnServerWide in deviceAdministration appliesTo {
    principal: Account,
    resource: Server,
    context: {},
  };
  action viewDeviceAdministration in deviceAdministration appliesTo {
    principal: Account,
    resource: Household,
    context: {},
  };
  action viewServerDeviceAdministration in deviceAdministration appliesTo {
    principal: Account,
    resource: Server,
    context: {},
  };
  action transferAccount in accountAdministration appliesTo {
    principal: Account,
    resource: Account,
    context: {},
  };
  action administrativelyDeleteAccount in accountAdministration appliesTo {
    principal: Account,
    resource: Account,
    context: { freshReauthentication: Bool },
  };
  action deleteMyAccount in accountAdministration appliesTo {
    principal: Account,
    resource: Account,
    context: { freshReauthentication: Bool },
  };
  action transferProfile in profileAdministration appliesTo {
    principal: Account,
    resource: Profile,
    context: {},
  };
  action administrativelyDeleteProfile in profileAdministration appliesTo {
    principal: Account,
    resource: Profile,
    context: { freshReauthentication: Bool },
  };
  action tearDownHousehold in householdAdministration appliesTo {
    principal: Account,
    resource: Household,
    context: { freshReauthentication: Bool },
  };
  action viewSecurityAudit in serverAdministration appliesTo {
    principal: Account,
    resource: Server,
    context: {},
  };
  action viewProfileAdministration in profileAdministration appliesTo {
    principal: Account,
    resource: Profile,
    context: {},
  };
}
