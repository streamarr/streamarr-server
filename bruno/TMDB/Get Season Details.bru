meta {
  name: Get Season Details
  type: http
  seq: 3
}

get {
  url: {{TMDB_BASE_URL}}/tv/1396/season/1
  body: none
  auth: inherit
}

headers {
  accept: application/json
}

tests {
  test("Should return TMDB season details", function() {
    expect(res.status).to.equal(200);
    expect(res.body.id).to.be.a("number");
  });
}
