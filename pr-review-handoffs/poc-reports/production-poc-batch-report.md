# Production LOC-Reduction POC Batch — verbatim agent report (archived for audit, E033)

Worktree of `codex/lean-playback-worker-adr` at `3d93dc41`; all measurements post-`spotless:apply` `git diff --numstat` totals (prod+test). "green" = exit code 0 plus per-class surefire/failsafe report files showing 0 failures/errors. Every POC was applied on a clean tree and reverted before the next. No commits; worktree left clean. POC patches were not preserved (the protocol reverts between POCs); re-derivation requires re-running the described edits.

> Note (E037): P01's verdict below was subsequently reclassified to POC-FAIL (interface) during adversarial re-review — the visibility widening it records as a caveat violates ledger rule 7 and the repo rule against test-driven visibility changes. The measurements stand.

| ID | Verdict | +add/−del (net) | Tests run (counts, all green unless noted) | Notes |
|----|---------|-----------------|--------------------------------------------|-------|
| P01 | POC-PASS (later reclassified, see note above) | +159/−185 (−26 total; prod −56) | Units: WorkerSessionServerTest 3, LiveWorkerConnectionRegistryTest 7, WorkerSessionGrpcServiceTest 6. ITs: WorkerSessionServerIT 27, RemotePlaybackIT 7, RemoteRecoveryIT 2, TranscodeWorkerIT 17, TranscodeWorkerApplicationIT 2, TranscodeWorkerKeepaliveIT 1. Full test-compile. | Delegation layer deleted; server keeps start/port/close + `connections()` accessor. Caveat the review missed: cross-package worker ITs (`com.streamarr.transcode.worker`) consume the delegates, so `LiveWorkerConnectionRegistry` had to become a public class with its 10 capability methods public (register/disconnect/release/upload-auth stay package-private) — operation surface count unchanged, but class visibility widens. Pre-start calls are now uniformly lenient (empty/false/0) instead of the old ISE-vs-lenient mix; `port()` stays fail-fast; `close()` now shuts down the executor a failed `start()` leaked. |
| P02 | POC-PASS | +22/−50 (−28, all prod) | ProducerLifecycleServiceTest 41, SegmentDeliveryCoordinatorTest 30 | `withSessionLock(UUID, Supplier)` + Runnable overload replaced all 7 lock/try/finally shells. Overload resolution is deterministic (void-body lambdas bind to Runnable, value-bodies to Supplier). No interface change. |
| P03 | POC-PASS | +39/−73 (−34; prod 0/−31 across 6 files) | Units: SegmentDeliveryCoordinatorTest 30, ProducerLifecycleServiceTest 41, HlsStreamingServiceTest 38, StreamControllerTest 36, LocalTranscodeExecutorTest 12, LocalFfmpegProcessManagerTest 16, LiveWorkerConnectionRegistryTest 7, WorkerSessionGrpcServiceTest 6. ITs: WorkerSessionServerIT 27, RemotePlaybackIT 7, RemoteRecoveryIT 2, TranscodeWorkerIT 17, HlsStreamingServiceIT 6. Full test-compile. | Verified zero production callers of one-arg `isRunning` outside the chain before deleting (interface, Local/Remote executors, WorkerSessionServer, registry+WorkerConnection, FfmpegTranscodeEngine, FakeTranscodeExecutor). `FfmpegProcessManager.isRunning(UUID)` retained per instruction. ~30 test call sites migrated to two-arg (single-variant sessions → `StreamSession.defaultVariant()`, fixtures → their job's label); one now-redundant "by session ID" LocalTranscodeExecutorTest test deleted. |
| P04 | POC-PASS | +17/−25 (−8, prod) | ProducerLifecycleServiceTest 41, SegmentDeliveryCoordinatorTest 30 | `baseRequest(session, seekPosition, startSequenceNumber)` builder shared by all 3 construction sites. Under the ~25 estimate because spotless re-wraps the replacementRequest call. 3 positional args is within repo rule. |
| P05 | POC-PASS | +16/−20 (−4; prod 0/−14) | WorkerSessionServerIT 27, TranscodeWorkerIT 17 (failsafe, exit 0) | Attempt-id `stopVariant` deleted from server+registry; 6 IT call sites migrated to (sessionId, variantLabel) — equivalent because each (session,label) has exactly one live attempt in those tests. `WorkerConnection.tryStop` kept. Test lines grew slightly from wrapping, production −14. |
| P06 | POC-PASS | +24/−35 (−11) | ProducerLifecycleServiceTest 41, SegmentDeliveryCoordinatorTest 30 | Sealed `ExhaustResult` → `boolean tryMarkExhausted(...)` (repo `try`-prefix rule); consumer switch collapsed to a single guard. 5 test assertions became isTrue/isFalse. |
| P07 | POC-PASS | +7/−15 (−8) | SegmentUploadAdmissionTest 12, SegmentUploadObserverTest 2 | `compute`/`boolean[1]` → `ConcurrentHashMap<UUID, Semaphore>` + computeIfAbsent/tryAcquire/release. Trade-off (recorded as code comment and here): entries are no longer removed at zero — one Semaphore per authenticated worker ever seen, bounded by the TLS-identity worker fleet, not upload volume. |
| P08 | POC-PASS | +15/−30 (−15) | SegmentDeliveryCoordinatorTest 30 | Two-line `recover()` inlined into `deliverOnce` (javadoc moved to `attemptReplacements`); three warn blocks collapsed to one switch-expression-driven `log.warn`. Log text changes slightly (STALLED message gains the "recovering across execution targets" tail); grep confirmed no test pins the messages. |
| P09 | POC-FAIL | +173/−40 (+133 net; prod alone +13/−16 = −3) | Units: RefreshTokenServiceTest 18, AuthSecurityEventLoggingTest 3, CreateAuthSessionCommandTest 2. ITs: AuthEndpointsIT 34, TokenRefreshTransactionIT 6, PasswordChangeLoginRaceIT 5, RefreshTokenReuseRevocationIT 2, RefreshRevocationRaceIT 2, RefreshRotationConcurrencyIT 2. All green. | The ~14-line estimate counted only the 2 production callers and ignored 19 test call sites across 8 files that use the `createSession(account, device)` convenience overload; each becomes a 3–5-line builder expression. Behavior stays green but total LOC rises sharply and call-site readability worsens. The overload is also within the repo's own 2–3-positional-arg rule, so there's no convention pressure to remove it. |
| P10 | POC-PASS | +12/−16 (−4, all prod) | Units: TranscodeWorkerContractTest 5, WorkerMediaSourceResolverTest 11, TranscodeWorkerSettingsTest 8. IT: TranscodeWorkerIT 17. Full test-compile. | Proto→domain container mapping now lives once in `WorkerVariantJobMapper.container` (widened private→package-private static); TranscodeWorker derives extension via `ContainerFormat.segmentExtension()` and content-type via an exhaustive 2-case switch — both required-throws deleted (unreachable: startVariant's map() already validated). Mappers NOT merged, per instruction. |
| P11 | POC-PASS | +34/−35 (−1 total; prod 0/−8) | Units: ProducerLifecycleServiceTest 41, SegmentDeliveryCoordinatorTest 30, SessionReaperTest 11, TranscodeHandleTest 3, StreamSessionTest 6, HlsPlaylistServiceTest 40, HlsStreamingServiceTest 38, StreamControllerTest 36. IT: HlsStreamingServiceIT 6. Full test-compile. | Both minting constructors deleted (the 3-arg had zero external callers); ~20 test sites migrated to `StreamSessionFixture.mintHandle(pid, status)`. Fencing note as predicted: production can now only obtain handles whose attemptId flows from a `TranscodeRequest` (engine 4-arg / `remoteDispatch` / `withStatus`), so a randomly-minted attempt id can no longer bypass `expectedAttemptId` fencing. Measurement caveat: first unit-batch run exited 1 with the failure localized to SegmentDeliveryCoordinatorTest (a file untouched by this POC, timing-sensitive waits); identical batch re-run plus 2 isolated re-runs all green — exact first-failure detail lost to report overwrite, so treat as an unconfirmed pre-existing flake, not cleared evidence. |
| P12 | POC-FAIL (LOC criterion) | +10/−10 (net 0; prod −6) | LocalSegmentStoreTest 17, full test-compile | Total LOC is flat, not falling: the 4 call sites (2 inside assertThatThrownBy lambdas) need a private test helper that is the same shim relocated. Redeeming data: production −6 and the `SegmentStore` interface narrows by one default method — worth doing for the interface, not for LOC. |
| P13 | NOT-EXECUTED | — | — | Explicitly excluded (already done elsewhere). |
| P14a | POC-PASS | +1/−2 (−1) | HlsStreamingServiceTest 38 | `Collections.emptyList()` → `List.of()`; `Collections` import dropped. |
| P14b | POC-PASS | +12/−16 (−4) | WorkerSessionGrpcServiceTest 6 | Named class + 3 identical comments → one shared `static final StreamObserver<UploadSegmentRequest> IGNORED_UPLOAD_OBSERVER` (anonymous, stateless) with a single doc line. |
| P14c | POC-FAIL (LOC criterion) | +2/−2 (net 0) | SegmentUploadObserverTest 2, WorkerSessionGrpcServiceTest 6 | LOC flat; the gain is deduplication only — the magic `"default"` in the upload path now reads from `StreamSession.defaultVariant()` (adds a domain import to the remote package, which is allowed). |
| P14d | POC-FAIL (LOC criterion) | +11/−9 (+2 net) | RemoteTranscodeConfigurationTest 4 (incl. context-runner startup tests — `@Builder` does not disturb `@ConfigurationProperties` record binding) | Only ONE 8-positional-arg construction exists, not several; builder migration adds lines. It's a repo-convention alignment (builders over >3 positional args), not a LOC reduction — justify it on that basis or skip. |
| P15 | NOT-EXECUTED (here) | +0/−7 (measured elsewhere) | 12 authority ITs green in main checkout | Skipped per coordinator update: measured and finalized as POC-PASS in the main checkout, whose newer authority-switch test this worktree lacks; final sign-off happened there. Schema basis checked here before the skip: V045's composite SET-NULL FKs (`active_membership`, `active_account_profile`, `active_profile_household`) plus V044's `account_profile` FKs make the two joins implied by the `ACTIVE_*` predicates on any live row. |

Aggregate of PASSing POCs as originally reported (P01–P08, P10, P11, P14a, P14b): net −144 total lines, ~−183 production lines, against the review's ~250+ estimate for the same items — estimates were directionally right but ~25–40% high, mostly from spotless re-wrapping and test-side migration growth. The three clear over-claims are P09 (+133 net — 19 uncounted test callers), P14d (+2 net), and P12/P14c (flat). One structural caveat worth surfacing on P01: the LOC win is real but costs `LiveWorkerConnectionRegistry` its package-private visibility (or alternatively a ~50-line test-scope shim that would erase the saving).
