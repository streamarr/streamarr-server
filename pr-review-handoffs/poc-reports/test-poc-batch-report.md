# Test-Consolidation POC Batch — verbatim agent report (archived for audit, E034)

Worktree `agent-ab70568d1239d9cc3`, base commit `3d93dc41`. All "before" counts measured on the clean tree; all runs judged by exit codes + surefire/failsafe XML report files. Diffs measured after `spotless:apply`. "mod" = insertions/deletions in modified files (`git diff --numstat`); "new" = lines in newly created file(s); net = mod-added + new − mod-deleted. Worktree reverted to pristine `3d93dc41` (nothing committed, nothing pushed). POC patches were not preserved (the protocol reverts between POCs); re-derivation requires re-running the described consolidations.

| ID | Verdict | Measured LOC (+added/−deleted) | Test classes run (before → after counts) | Notes |
|----|---------|-------------------------------|------------------------------------------|-------|
| T01 shared RemoteWorkerFixtures | POC-PASS | mod +35/−208, new fixture 64 → net −109 | WorkerSessionServerIT 27→27, RemotePlaybackIT 7→7, RemoteRecoveryIT 2→2, TranscodeWorkerIT 17→17, TranscodeWorkerKeepaliveIT 1→1, TranscodeWorkerApplicationIT 2→2; all green | The six `server()`/`worker()`/`resource()`/TLS blocks were byte-near-identical as claimed; fixture provides `tlsResource`, `tlsIdentity(cert,key)`, `serverConfigurationBuilder()`, `workerConfigurationBuilder()`, `remuxEngine(pm)`. Est ~240 overstates net (gross deletions were 208). |
| T02 FakeStreamingService | POC-PASS | mod +38/−262, new fake 72 → net −152 | SessionReaperTest 11→11, StreamingShutdownHookTest 2→2, StreamControllerTest 36→36 (unit); RemotePlaybackIT 7→7, StreamControllerIT 13→13 (IT); all green | Registry-backed fake (`FakeStreamingService(FakeRuntimeStreamSessionRegistry)`) reconciles all five stubs, incl. SessionReaperTest's shared-registry requirement and ShutdownHookTest's destroyedIds tracking; `createSession` throws (proven uncalled by any of the five). A sixth inner stub exists in StreamingSessionCleanupListenerTest (not in claim, not touched). |
| T04 StreamingRigFixture | POC-PASS (marginal LOC) | mod +46/−111, new fixture 59 → net −6 | SegmentDeliveryCoordinatorTest 30→30, ProducerLifecycleServiceTest 41→41, SessionReaperTest 11→11, HlsStreamingServiceTest 38→38, StreamControllerTest 36→36; RemotePlaybackIT 7→7, RemoteRecoveryIT 2→2; all green | Lombok `@Builder` static method returning a `StreamingRig(lifecycle, coordinator)` record; null clock→systemUTC, null pollInterval→coordinator's own default (preserves HlsStreamingServiceTest's unset-pollInterval behavior). LOC is a wash (est ~135 badly overstated); the win is 7 sites sharing one wiring, purely navigational. |
| T05 FakeAuthorizationService | POC-PASS | mod +19/−121, new fake 77 → net −25 | StreamControllerTest 36→36; RemotePlaybackIT 7→7; all green | Supplier-based identity constructor covers StreamControllerTest's mutable `boundStreamSession`. Reconciled diffs (tokenExpiry throw→now+3600, canViewActivityOf false→true, require* throw→delegate) are all on uncalled paths — proven by green runs. |
| T06 same-file helper usage | POC-PASS | +36/−186 → net −150 (no new files) | HlsStreamingServiceTest 38→38; WorkerSessionServerIT 27→27; all green | 13 inline MediaProbe blocks → `defaultProbeBuilder()` + exact-value overrides; 7 inline defaultOptions bodies → `defaultOptions()`; 8 inline gRPC status assertions → `assertUploadRejected` (widened to `CompletableFuture<?>` + `ThrowingCallable` overload to cover register-futures and `awaitClosed`). Best LOC/effort ratio of the batch. |
| T07 startedAbrSession → StreamSessionFixture | POC-PASS | +22/−37 → net −15 | SegmentDeliveryCoordinatorTest 30→30, ProducerLifecycleServiceTest 41→41; green | Blocks confirmed byte-identical. Only the ABR variants-builder moves (as `abrSessionBuilder()`); the registry.save+startAll lines are rig-bound and must stay per file, so est ~58 was ~4× the achievable net. |
| T14 FfmpegCommandBuilderTest builder fixture | POC-FAIL | +398/−188 → net +210 | FfmpegCommandBuilderTest 42→42; green | Mechanically works (JobSpec `@Builder` record + `jobSpec()` pre-populated builder, 40 call sites converted), but google-java-format reflows every 1-line positional call into a ~7-line builder chain; file grows by ~30%. House-rule compliance here is directly LOC-negative; est ~70 savings is refuted. |
| T11 shared MutableClock | POC-PASS | +12/−57 → net −45 | SegmentDeliveryCoordinatorTest 30→30, SegmentUploadAdmissionTest 12→12; green | Shared fake gained no-arg ctor (frozen at 2026-01-01) + `advance(Duration)` via `AtomicReference.updateAndGet` — strictly safer than both private copies (one wasn't even volatile). Additive change; 6 pre-existing user classes compile untouched. |
| T12 ProtoUuid static imports | POC-PASS | +63/−74 → net −11 | WorkerMediaSourceResolverTest 11→11; TranscodeWorkerIT 17→17, WorkerSessionServerIT 27→27; green | 43 call sites split into `toProto` (19) / `fromProto` (24) by argument direction. Longer names trigger line rewraps, eating most of the LOC gain. TranscodeWorkerContractTest's independent copy kept as instructed. |
| T09 TokenTestSupport | POC-PASS | mod +20/−104, new support 42 → net −42 | StreamingResolverTest 15→15, AccessTokenIssuerTest 12→12, PlaybackTokenIssuerTest 4→4, TokenCryptoConfigTest 24→24; AuthEndpointsIT 34→34; all green | Correction to the claim: the exact TEST key exists in 4 java files (not 7 — JwksControllerTest uses a different key, only the ASN.1 prefix matches). AuthEndpointsIT on the real `@Autowired JwtDecoder` passes 34/34 — its hand-rolled claims-skipping decoder was unnecessary. |
| T08 matrices → SegmentUploadObserverTest | NOT-EXECUTED | — | (baselines exist: WorkerSessionServerIT 27, SegmentUploadObserverTest 2) | `authorizesUpload` gate precedes `isValidMetadata` in SegmentUploadObserver, so a unit-level move needs new LiveWorkerConnectionRegistry scaffolding (register connection + dispatched variant state) — genuine test design, not mechanical consolidation, and per-class counts change by definition. Verified the claimed duplication is real: the 16MiB+1 pin appears in both matrix blocks (lines ~504 and ~600). |
| T10 core dedup (3 files) | POC-PASS | +10/−82 → net −72 | HlsPlaylistServiceTest 40→40, SegmentDeliveryCoordinatorTest 30→30, ProducerLifecycleServiceTest 41→39 (designed fold); all green | The refuses-test body was byte-identical to `exhaustRecovery` → delegated. `createSession` now delegates to `createSessionWithDuration` (33-line clone). Resume fold: 2 of the 3 folded tests were exact duplicates of the existing `segment5.ts,5,30` CSV row; net new row `segment0.ts,0,0`; the `isRunning` assertion moved into the parameterized test (now checked on all 4 rows — stronger). No assertion lost. |
| T13a playbackBearer → AuthTestSupport | POC-PASS | +39/−52 → net −13 | SecurityFilterChainIT 14→14, StreamControllerIT 13→13; green | `playbackBearer(TestIdentity, UUID)` added to AuthTestSupport (2 new injected deps: JwtDecoder, PlaybackTokenIssuer via AuthTestSupportConfig); both private copies + their now-orphaned `@Autowired` fields deleted. Other AuthTestSupport consumers compile (full test-compile) and share the same full-app context. |
| T13b AuthenticatedIdentityFixture | POC-PASS (partial: 10 of ~21 blocks) | mod +17/−81, new fixture 25 → net −39 | AuthenticatedIdentityTest 11→11, StreamControllerTest 36→36; green | Converted the worst file (9 blocks, each violation now expressed as `.field(null)`/override on a valid default) + StreamControllerTest's block. Finding: ~11 remaining blocks (PlaybackTokenIssuerTest, IdentityQueryServiceTest, …) pin specific ids, where the fixture barely shortens code — full ~21-block savings are overstated. |
| T13c LogCaptor | MEASURED — LOC-neutral, correctness win | mod +25/−36, new captor 36 → net +25 for 1 of 13 sites | WorkerIdentityServerInterceptorTest 5→5; green | try-with-resources indentation gives back most lines the attach/detach removal saves; extrapolated across all 13 ListAppender files (claim said ~9) it is roughly LOC-flat. Real value: the interceptor test's appender leak (never detached) is fixed by construction. |
| T13d fold MembershipTokenRevocationIT | NOT-EXECUTED | — | — | Requires physically merging two full-context IT classes plus another slow AuthEndpointsIT run; counts change by design. Remaining budget was spent on T15/T03 per "rigorous on fewer" guidance. |
| T15 drop AbstractIntegrationTest (runtime) | POC-PASS | wall-clock 25.87s → 15.19s (−10.68s, −41%); LOC +3/−6 | WorkerSessionServerIT 27→27, RemotePlaybackIT 7→7, RemoteRecoveryIT 2→2; green | None of the three files contains `@Autowired`/`@TestBean`; they compile and pass with no Spring context — the claim "never touch a Spring bean or the DB" is confirmed. Both runs shared the already-running reusable PG container, so the delta measures Spring-context boot only; a cold environment saves more. |
| T03 TranscodeWorkerIT merges | POC-PASS (4 of 5 sub-changes) | +19/−80 → net −61 | TranscodeWorkerIT 17→15 (2 designed merges); green | Assertion enumeration: deleted smoke asserted {dispatch true, started-contains} — both verbatim in the kept worker-close test; deleted stop-on-disconnect asserted {dispatch true, started-contains, stopped-contains} — all verbatim in the kept abandoned-job-logging test. `EndingFfmpegProcessManager` now extends the shared `FakeSegmentProducingFfmpegProcessManager` (had to drop its `final`) + the one `stopProcess` line; 13 media-root preambles → `preparedMediaRoot()`. The TS-upload/completion shared-fixture sub-claim (~305-369) was not applied (unmeasured). |

Aggregate of PASSing LOC POCs (excluding T14's +210 and T15's runtime-only change): ≈ −740 net LOC. Every consolidation was reverted after measurement; the worktree ends clean at `3d93dc41` with no commits.
